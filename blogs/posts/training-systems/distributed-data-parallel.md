# Data Parallel (DP)

一开始用在多核 CPU 当中，后为了兼容多卡 GPU 训练，直接将这个方法应用在 Pytorch 中，实际效果较差，现在 Pytorch 官方已强烈不建议使用这种方法，但为了知识体系的完整性，在介绍分布式计算前先简单了解 Data Parallel 的基本思想以及它存在的问题：

将程序运行在一个进程中，这个进程管理多个线程，在 CPU 中，这些线程共享同一片内存
